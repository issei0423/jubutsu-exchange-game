# CLAUDE.md

## プロジェクト概要

呪物交換対戦ゲーム(仮) — 1対1で呪物を取引し合うボードゲーム風対戦Webゲーム。
最終ターン終了時の**所持金**が多い方が勝ち。Web(PC/スマホ両対応)、オンライン対戦あり。

仕様・ルールの正は **`要件定義書.md`**。このファイルはその要約ではなく、
「作業のしかた」と「実装で踏み外しやすい点」だけを書く。細部は必ず要件定義書を読むこと。

## 作業ルール(重要)

- **作業(タスク)ごとに必ず新しいブランチを切り、`git worktree` で作業ツリーを分けること。** main ブランチ上で直接作業しない。
- ブランチ名は `feature/<内容>` / `fix/<内容>` / `docs/<内容>` のように内容がわかる名前にする。
- 手順の例:

```bash
# 作業開始: ブランチ + ワークツリーを作る
git worktree add ../jubutsu-feature-board -b feature/board
cd ../jubutsu-feature-board
# ...作業してコミット...

# 作業完了: main にマージしたらワークツリーを片付ける
git worktree remove ../jubutsu-feature-board
```

### ドキュメントの扱い

- `要件定義書.md` は**依頼者ヒアリングの記録**。実装の都合で勝手に書き換えない。
  変更したいときは、何をどう変えるかを先に提案して確認を取る。
- **【推測】** と付いている箇所は依頼者の確認が取れていない。確定事項として扱わない。
- 未定項目(要件定義書末尾の一覧・16件)を実装の勢いで確定させない。
  値が必要になったら**仮の定数として1か所にまとめ**、未定であることをコード上に残す。

## 用語(コード上の名前を揃える)

仕様の日本語とコードの識別子がズレると事故る。以下で統一する。

| 仕様 | コード | 補足 |
|---|---|---|
| 呪物 | `relic` | いわくつきの骨董品 |
| 価値 | `value` | 見た目では分からない隠し値 |
| 呪い | `curse` | 効果文は隠し情報 |
| 所持金 | `money` | マイナスもあり得る |
| ターン | `turn` | プレイヤー1人ぶんの手番 |
| 一区切り | `round` | ターン→ターン→呪物流し で1 round |
| 呪物が流される | `drift` | round 末尾のフェーズ |
| 自陣 | `field` | プレイヤーごと。盤の両端で相対 |
| フリー地 | `freeZone` | どちらの陣でもない領域 |
| 取引/鑑定/解析 | `trade` / `appraise` / `analyze` | ターン内行動。各1回まで |
| 予約 | `reservation` | `trade` は即時配置されない |
| 先攻トークン | `initiativeToken` | 3で発動しリセット |

## 絶対に壊してはいけない不変条件

このゲームは**隠し情報の非対称性**が面白さの本体。ここを崩す実装は仕様違反として扱う。

### 1. 真値はサーバーだけが持つ

- 呪物の `value`、`curse` の効果文、相手の `reservation` は**サーバーのみが保持**する。
- クライアントへは「そのプレイヤーが見てよい情報だけに射影した状態」を送る。
  **DevTools のネットワークタブを見れば分かる、はバグ。**
- クライアントは表示と入力だけ。判定・抽選・状態遷移はすべてサーバーで行う。

### 2. 情報公開のルール

| 行動 | 判明するもの | 見える範囲 |
|---|---|---|
| 鑑定 `appraise` | その呪物の `value` | **両プレイヤー** |
| 解析 `analyze` | その呪物の `curse` の効果文 | **解析した側のみ** |

- 予約は原則**相手から不可視**。所持金は原則**相手から見える**（配置時の増加量から価値を推測できるのは意図通り）。
- ただし上記はどちらも**呪いで例外を作れる**設計（相手の予約が見える呪い、自分の所持金を隠す呪いなど）。
  可視性を「常に真/常に偽」でハードコードせず、**呪いが上書きできる形**にしておく。
- 呪いは効果文が隠れていても、**振る舞い(現象)は相手にも観測できる**。
  現象そのものを不可視にしてはいけない。推測できることが遊びの核。

### 3. お金と盤面

- 自陣に配置 → `value` の分だけ所持金 **+**。自陣から外れる → 同じ分 **−**。
- **所持金はマイナスになり得る。0でクランプしない。**
- 敵陣の呪物を取引した支払いは**相手の所持金に入る**。消滅させない。
- `drift` では**盤上のすべての呪物**(自陣配置済みを含む)がプレイヤー側から中央へ1マス動き、中心で止まる。
- 自陣から外れた呪物はフリー扱いになり、**呪いは解ける**。

### 4. 順序と抽選

- round = 先攻ターン → 後攻ターン → `drift`。
- 先攻・後攻は round ごとにじゃんけん。連続先攻でトークンが貯まり、**3で発動 → リセット**。
  連続が途切れた場合も**リセット**。
- 発動した round では、先攻は鑑定・解析が封じられ、後攻は各2回行える。
- 取引は**必ず成立する(拒否権なし)**。予約は両者のターン終了後に**まとめて解決**する。
  同じ呪物に予約が被ったらじゃんけん(無条件で勝ち切る呪いあり)。
- じゃんけん(先攻決め/予約被り/引き分け決着)は**サーバーが裁定**する。クライアントの自己申告を信じない。
- パスは可能だが**警告を出す**。

## 呪いの実装方針

- MVP で **20種類程度**。効果は多彩で、発動タイミングも呪いごとに違う。
- ルールエンジン本体に呪いごとの分岐を書き散らさない。
  `配置時` / `予約解決時` / `drift 前` / `ターン終了時` / `ゲーム終了時` のような**フックにぶら下げる**形にし、
  呪い1つを1定義で完結させる。
- 想定例は要件定義書 §5「呪い」に8つある。残りを足すときも**既存の枠組みに載る形**で足す。

## スコープ

- **MVP は「友達と部屋を作って対戦」まで**(リンク共有など)。オンラインマッチングはその後。
- レート・ランク・アンロック・図鑑などのメタ要素は**持たない**。毎回まっさらな状態で対戦する。
- 1対戦 5〜15分程度。1ターンに持ち時間あり(長さは未定)。

## 技術方針(暫定)

正式決定ではないが、現時点の方向は以下。

- **バックエンド: Go** — WebSocket による**権威サーバー**。ゲーム状態を全部持つ。
- **フロント: TypeScript** — Next.js (React)。表示と入力のみ。
- 見た目は 2D(イラスト調/ドット絵は未定)。

言語が分かれるので、**型の定義を2か所で手書きしない**こと。
サーバー↔クライアント間のメッセージスキーマは1か所を正とし、Go/TS 両方の型を生成する。
どのスキーマ方式にするかは未決定。

Go 側の書き方の指針:

- **1対戦 = 1 goroutine**。ゲーム状態はその goroutine だけが触り、入力は channel で受ける。
  状態を mutex で共有しない(隠し情報の漏れとデータ競合の温床になる)。
- 状態遷移は純粋な関数に寄せ、I/O(WebSocket)と分離してテストできるようにする。
- クライアントへの送信は必ず**プレイヤー視点への射影を通す**。内部状態をそのまま marshal しない。
- じゃんけん等の乱数はサーバー側で引く。

**この節は確定ではない。** 実装に入る前にスタックを確定させる会話を挟むこと。
確定したらこの節を書き換え、要件定義書 §11 と未定リスト #12 も併せて更新する。
